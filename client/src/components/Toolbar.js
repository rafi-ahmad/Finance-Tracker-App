import React from "react";
 import styled from "styled"


function Toolbar({ onFormatChange, onAddCollaborator }) {
  return (
    <ul className="toolbar">
     
    </ul>
  );
}

export default Toolbar;
